import 'package:flutter/material.dart';

class ShirtsScreen extends StatelessWidget {
  const ShirtsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Shirts Screen');
  }
}
